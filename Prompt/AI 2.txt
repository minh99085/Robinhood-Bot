
Role: You are Grok Build acting as senior quant developer + trading-systems engineer. Your mission is to make Grok-Bot-2 materially more profitable in paper trading by shifting the bot from weak directional prediction to arbitrage-first mathematical infrastructure.

Goal:
Target a 5x improvement in paper-trading edge quality versus the current baseline, measured by segregated arbitrage PnL, after-cost EV, executable VWAP profit, and reduced directional bleed. Do not claim 5x success unless the replay/report proves it. Keep all live trading disabled.

Context from docs/polymarket-arbitrage:

* Current engine is already sophisticated: multi-loop architecture, risk-free arbitrage ledger, Grok shadow analyst, verifier, lessons/research_meta loops, execution gates, reconciliation.
* Current strongest edge is arbitrage, not directional prediction.
* Previous reports show arb produced most profit, while directional is weak/coin-flip.
* The Polymarket Arbitrage Bible says durable edge requires: market graph, dependency detection, integer/linear constraints, Bregman/KL projection, Frank-Wolfe/IP solver when needed, VWAP/depth execution validation, non-atomic execution simulation, and modified Kelly sizing.
* Do not overbuild Gurobi/Frank-Wolfe for trivial single 2-outcome windows. Use it only for dependent windows/correlated groups.

Hard rules:

* PAPER ONLY.
* Do not enable live orders.
* Do not add private keys or secrets to git.
* Do not let Grok create trades directly.
* Grok may screen dependencies, explain evidence, veto risk, and summarize reports only.
* Every strategy must have separate ledger, PnL, hit rate, drawdown, and readiness status.
* Directional cannot hide behind arbitrage profits.

Implement in priority order:

1. Make arbitrage the primary book
   Set primary_edge_source="arbitrage" unless reports prove another strategy is superior.
   Create/repair dedicated modules if missing:

* arb_graph
* arb_constraints
* arb_scan
* arb_execution_sim
* arb_replay
* arb_report

Directional BTC Pulse must be capped or observe-only until it proves positive expectancy after costs with enough sample size.

2. Scale proven within-window arbitrage safely
   Improve the existing buy-both/sell-both dutch-book scanner:

* Scan all active BTC 5m and 15m up/down windows each cycle.
* Add ETH 5m/15m only if market discovery and book quality are reliable.
* Compute executable VWAP for both legs from full order book depth, not only best bid/ask.
* Accept only if guaranteed profit after fees, slippage, latency, and non-atomic execution buffer exceeds configured threshold.
* Keep 50% depth cap.
* Add near_miss logging for opportunities that almost passed.

Acceptance:
Report must show arb_scan_count, arb_candidates, arb_executable, arb_rejected_by_reason, arb_realized_pnl, and arb_near_miss.

3. Add linear-constraint arbitrage scanner
   Implement fast LCMM-style checks before any heavy solver:

* YES + NO below redemption value: buy both.
* YES + NO above redemption value: simulated mint/sell-both opportunity.
* Complete mutually-exclusive outcome group checks.
* Subset/implication checks: if A implies B, then P(A) cannot exceed P(B).
* Contradiction checks across related markets.

Start with deterministic rule templates, not LLM-only reasoning.

Acceptance:
Replay must show each candidate’s constraint_type, affected_markets, legs, executable_vwap_cost, guaranteed_profit, and rejection reason.

4. Add market graph and dependency discovery
   Build a canonical market graph:

* condition_id nodes
* token/outcome nodes
* event/series clusters
* time-window family links
* BTC 5m -> BTC 15m nested-window candidates
* BTC/ETH correlated candidate groups
* manually known implication/subset/mutual-exclusion rules

Use Grok only to propose candidate dependencies from market titles/descriptions into structured JSON.
Then validate dependencies deterministically before they become tradable.

Acceptance:
Report dependency_proposals, deterministic_validated_dependencies, rejected_dependencies, and why.

5. Add Bregman/KL projection layer for non-trivial groups
   Implement a projection engine only for groups with more than trivial 2-outcome structure.
   Start simple:

* KL/Bregman distance scoring
* small-group convex/iterative projection
* solver abstraction with brute-force for tiny groups and optional OR-Tools/PuLP/scipy fallback
* Frank-Wolfe scaffold with iteration logs, convergence status, active vertices, and solve time

Output:

* projection_distance
* optimal_trade_vector
* max_theoretical_profit
* executable_profit_after_depth
* solver_status
* convergence_reason

Do not route projection candidates to simulated trades until execution validation passes.

6. Strengthen non-atomic execution simulation
   Before accepting any multi-leg arb:

* Simulate leg 1 fill.
* Re-read/reprice leg 2.
* Compute worst acceptable second-leg price before leg 1.
* Reject if second leg cannot fill within max acceptable price.
* Model partial fills, stale book, spread expansion, and queue risk.
* Minimum guaranteed profit must survive the worst path.

Acceptance:
Report non_atomic_pass, leg_order_tested, worst_case_profit, partial_fill_exposure, unwind_required, and final decision.

7. Modified Kelly sizing for proven arbitrage only
   Add paper sizing that uses:

* guaranteed_profit percentage b
* probability of full execution p from depth/fill simulation
* q = 1 - p
* capped modified Kelly
* max 50% of eligible depth
* bankroll cap
* daily loss cap
* no martingale / no averaging down

Directional stays flat/observe-only until promoted.

8. Directional strategy demotion and proof gate
   Directional may only trade if:

* positive after-cost EV
* sample size threshold met
* Wilson lower bound passes
* profit factor > configured threshold
* average win >= average loss or sizing corrects asymmetry
* out-of-sample replay positive
* not dominated by arbitrage opportunity

Otherwise:
directional_strategy_status="observe_only_required"

9. Reporting upgrade
   Regenerate reports from the same source of truth.
   Add:

* primary_edge_source
* arb_pnl
* directional_pnl
* reward_mm_pnl if any
* total_pnl_by_strategy
* trade_rate_by_strategy
* PnL by constraint_type
* PnL by projection_distance bucket
* PnL by execution_quality bucket
* PnL by settlement_source
* near_miss table
* 5x_improvement_status

FULL_REPORT.md must match JSON reports exactly.

10. 5x improvement test
    Define baseline from existing reports:

* Current proven arb baseline is small but positive.
* Directional is weak and must not be scaled.

Run replay/verify and report whether the new engine achieves:

* at least 5x candidate quality improvement OR 5x paper arb PnL per comparable window set,
* positive total segregated arbitrage PnL,
* lower directional drawdown,
* no readiness claim if evidence is insufficient.

If 5x is not proven, write:
five_x_improvement_status="not_proven_yet"
and list the top three blockers.

Acceptance tests:

* Paper-only safety remains enforced.
* Live order routing remains disabled.
* Grok cannot create trades.
* Directional cannot hide losses behind arb profits.
* Arbitrage ledger is segregated.
* VWAP/depth is used for every arb candidate.
* Non-atomic execution simulator can reject fake paper arb.
* cex_proxy/weak settlement source cannot create readiness.
* FULL_REPORT.md and JSON metrics match.
* run_ready_for_hours=false unless all safety, edge, and reconciliation gates pass.

Verification commands:

* Run unit tests.
* Run paper verification.
* Run replay on existing report data.
* Generate updated FULL_REPORT.md and JSON artifacts.
* Push to GitHub main.
* Report final commit hash.

Final response must include:

* final commit hash
* files changed
* tests run
* before vs after metrics
* arb_pnl
* directional_pnl
* five_x_improvement_status
* run_ready_for_hours
* top remaining blocker
